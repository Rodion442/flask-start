# flask-start

Zaawansowane aplikacje webowe - Flask: start.
Pierwsza aplikacja Flask: trasy tekstowe, statusy HTTP i JSON.

## Uruchomienie
```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python app.py